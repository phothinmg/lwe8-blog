import Showdown from "showdown";
function addListener(className) {
    if (typeof global !== "undefined" &&
        global.copyCodeListener !== true) {
        global.addEventListener("click", function (e) {
            var _a, _b;
            var target = e.target;
            if (((_a = target === null || target === void 0 ? void 0 : target.classList) === null || _a === void 0 ? void 0 : _a.contains(className)) &&
                ((_b = target.nextElementSibling) === null || _b === void 0 ? void 0 : _b.tagName) === "PRE") {
                navigator.clipboard.writeText(target.nextElementSibling.innerText);
            }
        });
        global.copyCodeListener = true;
    }
}
/**
 * showdownCopyCode
 *
 * Showdown extension that adds a button above code blocks to quickly and easily copy code to the clipboard.
 *
 * @function
 */
function copyCode(_a) {
    var _b = _a === void 0 ? {} : _a, _c = _b.className, className = _c === void 0 ? "copy-code" : _c;
    return [
        {
            type: "output",
            filter: function (text) {
                addListener(className);
                return text.replace(/<pre.*><code/g, "<button class=\"".concat(className, "\">Copy</button>$&"));
            },
        },
    ];
}
Showdown.extension("copyCode", copyCode());
export default copyCode;
//# sourceMappingURL=copy-code.js.map